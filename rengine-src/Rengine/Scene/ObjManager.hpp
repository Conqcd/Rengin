#pragma once
#include <string>

namespace Rengin
{
class ObjManager
{
private:
public:
    ObjManager(const std::string& path,const std::string& materialpath);
    ~ObjManager();
};

} // namespace Rengin
